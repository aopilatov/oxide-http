# Alpine (musl). A musl binary is required here: the addon's libc must match Node's
# libc, otherwise loading fails with a linking error.
FROM node:22-alpine

WORKDIR /app
COPY package.json index.js index.d.ts ./
COPY dist ./dist
COPY *.node ./
COPY scripts ./scripts

RUN node scripts/smoke.cjs
