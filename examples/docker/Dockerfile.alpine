# Alpine (musl). Здесь нужен именно musl-бинарник: libc аддона обязана
# совпадать с libc Node, иначе загрузка падает с ошибкой линковки.
FROM node:22-alpine

WORKDIR /app
COPY package.json index.js index.d.ts ./
COPY dist ./dist
COPY *.node ./
COPY scripts ./scripts

RUN node scripts/smoke.cjs
